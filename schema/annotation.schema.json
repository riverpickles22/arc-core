{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "annotation.schema.json",
  "title": "Manuscript annotation",
  "description": "A thought the author had while reading a specific passage (conventions \u00a714). Anchored to prose, not to canon. Distinct from material: material is an idea that may enter the story; an annotation is a reaction to what is already on the page.",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^note\\.[a-z0-9-]+$"
    },
    "anchor": {
      "type": "object",
      "description": "What the note is about. A passage note carries scene, paragraph and quote \u2014 resolved in that order of durability. A scene note carries the scene alone: it is about the section rather than a sentence in it, which is the only shape an observation about something ABSENT can take, since absent text cannot be quoted. A scene note can never drift and never orphan; only deleting the scene breaks it.",
      "properties": {
        "scene": {
          "type": "string",
          "pattern": "^sc\\.[a-z0-9-]+$"
        },
        "paragraph": {
          "type": "integer",
          "minimum": 0,
          "description": "0-based paragraph index when the note was made. Absent means the note is about the whole scene."
        },
        "quote": {
          "type": "string",
          "description": "The selected text, verbatim \u2014 the durable fallback when indices shift. Requires a paragraph: a quote with no index is an incoherent anchor, not a scene note."
        }
      },
      "required": [
        "scene"
      ],
      "dependentRequired": {
        "quote": ["paragraph"]
      },
      "additionalProperties": false
    },
    "body": {
      "type": "string",
      "description": "The note itself, in the author's words. Never categorised by them."
    },
    "status": {
      "enum": [
        "open",
        "working",
        "resolved",
        "dropped"
      ]
    },
    "created_at": {
      "type": "string"
    },
    "links": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "What this note became when its scope outgrew the passage \u2014 material, obligation, or canon ids."
    },
    "note": {
      "type": "string"
    }
  },
  "required": [
    "id",
    "anchor",
    "body",
    "status"
  ],
  "additionalProperties": false
}
